# henji 用例集

### コードを改善する

ソースコードをhenjiにパイプで渡し、何をしてほしいか指示するだけで、リファクタリング・機能追加・デバッグの選択肢が大きく広がります。

`henji -f "このコードを改善するとしたらどう思う？" < main.go`

### 新機能のアイデアを出す

henjiはソースコード（やREADMEファイル）を元に、全く新しい機能案を考えることもできます。

`henji -f "このツールに10個の新機能を提案して" < main.go`

### ドキュメント作成を手伝ってもらう

henjiは新しいドキュメントの下書きをすぐに作ってくれます。

`henji "rキーを押すと無料でウサギが送られてくる機能について、このreadmeに新しいセクションを書いて" < README.md`

### 動画ファイルを整理する

ファイルシステムはhenjiへの入力として非常に優秀な情報源になります。音楽や動画ファイルがあれば、henjiは`ls`の出力を解析して内容をうまく整理・要約してくれます。

`ls ~/vids | henji -f "これらを年代別に整理してそれぞれ要約して"`

### おすすめを生成してもらう

henjiは手元にあるものを元に、似たジャンルだけでなく、全く別のメディア（例えば持っている映画から音楽のおすすめを出す、など）でもおすすめを生成するのが得意です。

`ls ~/vids | henji -f "これらを元に10個の番組をおすすめして、あまり有名でないものにして"`

`ls ~/vids | henji -f "これらの番組を元に10枚のアルバムをおすすめして、サントラや番組内で使われた曲は除外して"`

### 運勢を占ってもらう

ダウンロードフォルダはいつの間にか収拾のつかないファイルの山になりがちですが、henjiがあればそれを逆手に取れます！

`ls ~/Downloads | henji -f "このファイル群を元に私の運勢を占って"`

### APIを理解する

henjiは`curl`によるAPI呼び出しの出力を解析し、人間が読みやすい形に変換できます。

`curl "https://api.open-meteo.com/v1/forecast?latitude=29.00&longitude=-90.00&current_weather=true&hourly=temperature_2m,relativehumidity_2m,windspeed_10m" 2>/dev/null | henji -f "この気象データを人向けに要約して"`

### コメントを読む（自分で読まなくて済むように）

APIと同じように、henjiは生のHTMLを読んで内容を要約できます。

`curl "https://news.ycombinator.com/item?id=30048332" 2>/dev/null | henji -f "コメントの投稿者たちは何を言っている？"`

### コミットメッセージを考えてもらう

ステージ済みの差分をhenjiに渡して、コミットメッセージの候補を出してもらいます。`--output json`にしておけば、gitのhookなど他のスクリプトが生のテキストを解析せずに提案を埋め込めます。

`git diff --staged | henji --output json "この差分に対するコミットメッセージを3つ提案して" | jq -r '.content[0].text'`

### 巨大なログファイルを開かずに把握する

サーバーログは肥大化しがちですが、henjiに丸ごと渡せば、自分のエディタ（やAIエージェント自身のコンテキスト）に読み込むことなく、要点だけを受け取れます。

`cat huge_server.log | henji --output json "エラーパターンの上位5件とそれぞれの件数を教えて" | jq -r '.content[0].text'`

### 無料のローカルモデルで済ませてから、必要な時だけ課金モデルに聞く

まずローカルゲートウェイに質問を投げて、納得いかない時だけクラウドモデルにエスカレーションする、コスト意識の高い使い方です。

ここでの`ollama`は`henji.yml`で付けたAPI名に過ぎません。henjiはOllama専用クライアントではなく、OllamaのOpenAI互換`/v1`エンドポイント経由で通信します。

`echo "この正規表現を説明して: ^(?:[a-z0-9]+_)+[a-z0-9]+$" | henji --api ollama --model llama3.2 -f`

### プロジェクトの肥大化の原因を調べてもらう

MCPのファイルシステムツールを繋いでおけば、henjiは記憶からの当てずっぽうではなく実際にディスクを調べて、大きいファイルとその理由を説明してくれます。

`henji --max-tool-calls 5 "このプロジェクトで一番大きいファイルを教えて、それぞれ何のためのファイルか説明して"`

### 骨のある問題にじっくり考えさせる

DeepSeek R1のような推論系モデルは答える前に考える分、数学的な証明や込み入ったロジックの検証で明らかに丁寧な応答を返します。

`henji --api deepseek --model r1 --max-tokens 4096 "素数が無限に存在することを証明して、その上で自分の証明の穴を探して"`

### 同じ土台から複数の案を試す

会話を分岐させれば、同じ前提を説明し直すことなく、複数の切り口の答えを引き出せます。

```
henji --title "api-design" "TODOアプリのREST APIを設計して"
henji --continue "api-design" --title "graphql-take" "同じものをGraphQLで作り直して"
```
